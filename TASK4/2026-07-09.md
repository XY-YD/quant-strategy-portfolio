# 2026-07-09 工作日志

## TASK4 — 海龟交易策略（已完成）
- 新建 `/Users/wangyanfen/Desktop/量化策略课程/TASK4/`
- 交付物：`task4_turtle.py`（主实现）、`task4_report.py`（PDF 生成）、7 张 PNG、3 个 CSV、`夏阳+TASK4.pdf`（9 页）。
- 实现要点（简化但忠于原版的单系统）：
  - 唐奇安通道：`upper=最高价.rolling(N).max().shift(1)`，`lower=最低价.rolling(M=N//2).min().shift(1)`（避免未来函数）。
  - ATR：TR=max(高-低,|高-昨收|,|低-昨收|)，Wilder 平滑 `ewm(alpha=1/N)`。
  - 信号：收盘>上轨买入；持仓中 最低价≤买入价−2×ATR（盘中触及止损，以止损价成交）或 收盘<下轨 卖出；止损优先。
  - 回测：ATR 单单位仓位 `shares=floor(0.01×equity/ATR_entry)`，净值法算日收益；`calc_metrics` 复用 TASK3 逻辑（MDD/夏普/累计回报）。
- 关键结论（数据真实反映，非人造）：本周期 4 只股票中 3 只下跌，海龟核心价值是**下行风控**——把买持损失从 16%~39% 压缩到 3%~6%；宁德时代买持 +42.96% 但海龟因 10 日退出通道偏紧+仅做多，震荡期被洗，最终微亏(-0.02%)。参数 N=10 略优(+3.03%)，长周期 N=55 同样失效。
- 运行环境：Anaconda `/Applications/XYsPythonProject/anaconda3/bin/python`（reportlab 5.0.0 已具备）；中文 PDF 字体 `/Library/Fonts/Arial Unicode.ttf`。
- 注意：Glob 对中文路径返回空，扫描目录用 `find` 或 pandas 读取已知路径。
